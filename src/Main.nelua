##pragmas.nogc = true
##cflags '-Ofast'
## linklib 'glfw'

require "src/ECS"
--[=[]]
local fortnite = "amoddue"

local Position = @record{
	x: float32,
	y: float32,
	z: float32,
}
]=]

require("src/Components")
--local arch1: Position = {x = 1, y = 1, z = 1}